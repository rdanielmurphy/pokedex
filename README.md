# Capitalize Pokemon API take home

## Background
Uses pokeapi.co to create a Pokedex.  Uses localstorage to save favorites. Search by pokemon name, ability, or type.

## To Run
Run the following commands:

```npm install```

```npm start```